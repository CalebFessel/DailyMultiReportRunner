[pytest]
# The suite lives beside the code rather than in a tests/ directory, because
# the files are copied onto the reporting machine individually and a test that
# travels with them can actually be run there.
testpaths = .
python_files = test_*.py
addopts = -q
